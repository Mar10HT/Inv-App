export const environment = {
  production: true,
  apiUrl: 'https://inv-app-api-production.up.railway.app/api',
  // Sentry DSN for error tracking. Empty until a real Sentry project is provisioned.
  // e.g. "https://xxx@xxx.ingest.sentry.io/xxx"
  sentryDsn: ''
};
