export const environment = {
  production: false,
  apiUrl: 'http://localhost:3001/api',
  // Sentry DSN for error tracking. Empty by default (no-op) — set to enable.
  // e.g. "https://xxx@xxx.ingest.sentry.io/xxx"
  sentryDsn: ''
};
